// Para não utilizar esse arquivo que é um preset de configuração basta passar ele pelo comando
// do terminal ou pelo script do package.json 
module.exports = {
  presets: [
    ["@babel/preset-env"]
  ]
}